<%= form_with(model: [ @game, @game.guesses.build ] , local: true ) do |form| %>
  <p>
    <%= form.label :guess, "Guess a Letter" %>
    <%= form.text_field :guessed_letter %>
  </p>
  <p>
    <%= form.submit "Submit" %>
  </p>

  <% if flash[:success] %>
    <% if @game.guess_correct? %>
      <div class= "alert-success"><h3>Guess is correct</h3></div>
    <% else %>
      <div class= "alert-danger"><h3>Guess is incorrect</h3></div>  
    <% end %>  
  <% else %>
    <div class ="alert-danger"> <%= flash[:notice] %> </div>
  <% end %>
<% end %>